---
applyTo: "**/*.ts,**/*.tsx"
---

## TypeScript Rules

- Always use `const` over `let` where possible.
- Prefer `interface` over `type` for object shapes.
- Use explicit return types on all exported functions.
- Avoid non-null assertions (`!`) — use proper type narrowing instead.
